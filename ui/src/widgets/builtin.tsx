'use client'
// Generic stat/level/state widgets were removed in favour of purpose-built
// per-connector widgets (see connectors.tsx). This module is intentionally
// empty of registrations; kept as an import target so page imports stay stable.
export {}
